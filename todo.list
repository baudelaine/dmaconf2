Correction DMA 
- Hidden if no label (Mode deconnecté) -> A tester
- Chargement des labels csv (Lorsqu'on charge ensuit les labels des QS. 
  Le labels des champs fonctionne qd on le fait en premier (vérifier charser) -> ???
- % Mode connecté SQL server, et %par lien parent -> en attente d'une base SQL Server de test
- Remonter l'erreur comme avant dans le message rouge et dans le error.txt -> A faire
- SQL Label par QS -> ???
- Bug quand on a fait watch content et qu'on ajoute un ref ou final par la suite -> A tester
- Gérer la poubelle des final -> A tester
- Ajouter la tarification (Comtpe Objets DMA, Relation Ref, Niveau et Mesure) -> Quasi fini manque le metric pour les objects créé automatiquement 
- Pouvoir télécharger le model.xml à tout moment -> A tester
- Gérer le shutdown aléatoire -> A faire
- Search Tool en mode déconnecté -> A tester
- Ajouter indicateur DMA (Nb Fields in table) -> A tester
- Gérer les delim = ; avec des doubles quotes dans les descriptions
- Il manque des tables dans search tool (ORA deconnecté CCMO table STA_GRC_INTERACTION)
- IL manque des champs dans le DMA (ACTOR STA_GRC_INTERACTION)
- Labels table pas chargés

Import fichier labels, gérer double cote
Import fichier view, gérer double cote
Remove expression relations
Liste champs above par expression régulière depuis l'expression de relation
Ajouter une case à cocher dans l'onglet Ref - Invert Cardinality
Ajouter case à cocher (Label table parent)
setHidden (if no label) All
Ne pas fermer le panneau qd on chose une case ref que l'alias existe déjà
Créer un autre bouton Generate view (différent) pour un algo différent

en dev cool et rapide
setHidden (if no label) All
remove expression relation
remove views

Ajouter une case à cocher dans l'onglet Ref au niveau des relations - Invert Cardinality
Ajouter case à cocher dans l'onglet Final au niveau des relations - Right join
Ajouter case à cocher dans l'onglet QuerySubjet (table label in braket) au niveau du field (avec un cocher tout qqp) par ex en dessous du bouton setHidden All
Ajouter case à cocher dans l'onglet QuerySubjet (path label in braket) au niveau du QS (avec un cocher tout qqp) par ex en dessous du bouton setHidden All
Ajouter une case à cocher dans l'onglet Ref au niveau des relations - Invert Cardinality
Ajouter case à cocher dans l'onglet Final au niveau des relations - Right join
Ajouter case à cocher dans l'onglet QuerySubjet (table label in braket) au niveau du field (avec un cocher tout qqp) par ex en dessous du bouton setHidden All
Ajouter case à cocher dans l'onglet QuerySubjet (path label in braket) au niveau du QS 
deplacer le champ folder dans l'onglet final (de QS vers Final)

download model.json -> corriger qss doit etre querySubject

la fonction upload model.json ne fonctionne pas

table label et table description ne sortent pas dans l'extract des views.csv
le champ n'est pas rempli, mais les colonnes sont la

Est-ce possible d'ajouter une poubelle sur les Views (Au niveau du QS et du Field stp) Merci d'avance camarade. y a aucune règle, aucune prise de tete, juste un poubelle pour enlever un champ qu'on veut plus
et aussi un QS



